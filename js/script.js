const buttonA = document.getElementById("buttonA");
// Puedes agregar eventos a los botones aquí.

// Ejemplo de interacción:
buttonA.addEventListener("click", () => {
    // Lógica cuando se presiona el botón A.
});


//let buttons = document.querySelectorAll('.button');
//buttons.forEach(button => {
    button.style.transform = 'rotate(45deg)';
//});

//document.getElementById('arrowup').style.transform = 'rotate(0deg)';
//document.getElementById('arrowright').style.transform = 'rotate(90deg)';
//document.getElementById('arrowdown').style.transform = 'rotate(180deg)';
//document.getElementById('arrowleft').style.transform = 'rotate(270deg)';